### 项目协作规则
- 除非用户指定其他语言，否则默认使用中文回复。

### 数字精度处理
- 跨 JSON 的 uint64/int64 字段：UID、邮件ID、请求ID、游标等标识类必须编码为十进制字符串，前端禁止用 Number/parseInt 处理；
- 可计算的业务数字（等级、金币等）仅当能证明不超过 Number.MAX_SAFE_INTEGER 时才用 Number，否则用字符串+BigInt；
- 测试须覆盖 9007199254740992/993 相邻大整数，确保不被合并。

### Git提交规范
- 约定式前缀 + 中文描述的主题