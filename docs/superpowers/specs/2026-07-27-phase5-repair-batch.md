# 经典农场 · 期 5 规格：已完成修复批次

> 状态：历史修复已完成，待按本规格重构为可审查提交
> 工程规范：`docs/superpowers/specs/2026-07-26-engineering-standards.md`
> 问题来源：2026-07-27 审查汇总（已吸收，不再保留独立工作稿）
> 基线：`d35a4f0`（期 4 社交闭环合入）

---

## 1. 目标

将基线之后已在工作树中完成的可靠性、可扩展性与客户端韧性修复，作为一个**可审查、可验证、可回滚**的整合提交落库；不重新设计或追加功能。

每项修复必须具备：

1. 同一个中文 Conventional Commit 中的实现、测试、生成物和文档；
2. 一组覆盖所有修复的验证命令；
3. 可从本规格的条目追溯问题、边界及验收条件。

## 2. 范围

| 编号 | 已完成修复 | 验收结果 |
| --- | --- | --- |
| P0-1 | 008 邮件迁移形态判定、全量迁移执行 | 重跑迁移不删除已有邮件与附件 |
| P0-2 | 非开发环境密钥启动校验 | 弱或默认鉴权、邀请、草虫盐密钥拒绝启动 |
| P0-3 | Actor 周期写回、同步落盘与 SIGTERM 疏散 | 在线 Aggregate 有界时间内尝试持久化 |
| P0-4 | 跨农场访客预占持久化 | 冻结金币和 pending 在进程重启后可结算或超时回滚 |
| P1-1 | 主人侧跨农场裁决去全局锁与有界去重 | 不在全局锁内执行外部 I/O；缓存有界且复制地块数据 |
| P1-2 | 已提交动作的应答语义与 WS 慢写约束 | 辅助任务失败不覆盖已提交结果；写入有 deadline |
| P2-1 | 确定性草虫与权威地块状态 | 同一输入草虫结果可重放；健康、偷取量、施肥掩码下发并安全渲染 |
| P2-2 | CSV 双端作物配置 | CSV 为唯一来源；保留历史作物 ID；季长使用整数分钟 |
| P2-3 | 客户端超时、退避重连与房间恢复 | 超时可终结，请求断线可重试连接，重连后以 EnterFarm 权威状态恢复 |
| P2-4 | FarmDelta 单编码批量转发与严格信封校验 | 按 Gateway 分批；推送不接受尾随或错误元数据 JSON |
| P3-1 | 健康探针、指标、pprof 与结构化日志 | 业务端口外提供 liveness/readiness/metrics/pprof，关键路径可观测 |
| P3-2 | 农场热路径基准与零分配验证 | 核心推进、草虫扫描、产量计算有可证伪基准 |
| P3-3 | 连接成员级租约与重启安全连接 ID | Redis 过期成员可清理，Gateway 重启不复用活跃连接 ID |
| P3-4 | Three.js 与页面卸载资源生命周期 | RAF、监听、独占 GPU 资源和页面定时器可幂等释放 |
| 附属 | 根目录 demo 归档 | demo 随整合提交保存，不影响生产构建 |

## 3. 全局约束

- 这是**历史提交重构**：只暂存当前已有的 hunks、测试和生成物；不得借此实现 Claude 最终审查中的 Important 项或其他新行为。
- 不改写 `d35a4f0` 及更早的历史；从当前 `main` 的未提交工作树创建一个整合代码提交。
- 每个提交使用约定式前缀和中文主题；不使用 `--no-verify`。
- 代码提交以当前工作树的完整修复集为边界；不再按 `main.go`、`runtime.go`、`ws.go`、`client/src/game/main.js` 等文件的 hunks 拆分历史。
- `config/crops.csv`、生成器和两端生成物必须同提交；`make gen-check` 是 P2-2 的必过验证。
- `.superpowers/sdd/progress.md` 是本地账本，保持忽略，不提交。
- 删除已吸收的临时审查报告与一次性迁移助手；`demo/` 纳入整合提交，但保持与生产构建无引用关系。

## 4. 非目标

- 不修复尚未实施的持久化命令回执、跨 Gateway 故障切换、邮件跨介质原子提交或 Actor 内邮件数据库 I/O。
- 不改变跨农场客户端超时的“结果未知”产品语义。
- 不重构现有期 4 功能或历史提交。
- 不把 demo 迁入客户端或修改其玩法。

## 5. 完成标准

1. P0-1 至 P3-4 与 demo 全部包含在一条整合代码提交中，提交主题准确描述这批可靠性修复。
2. 服务端测试、客户端测试与构建、生成检查均通过。
3. `git status --short` 中不再出现已纳入整合提交的修复文件、旧审查报告或 `server/cmd/mig9/`。
4. 后续 Important 项保持未实现、未伪称已解决，并继续由新规格/计划管理。
