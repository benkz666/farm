apiVersion: v1
kind: Service
metadata:
  name: gateway-nodeport
  namespace: farm
  labels:
    app.kubernetes.io/name: gateway
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: gateway
  ports:
    - name: http
      port: 9002
      targetPort: http
      nodePort: 30002
